[parameters]

    ; general app 
    app.environment    = development   
    app.http.host      = localhost
    app.http.port      = 3000
    app.https.host     = localhost
    app.https.port     = 3001
    app.session.secret = not-secret-change-me

    ; redis
    redis.host         = localhost
    redis.port         = 6379

    ; mongodb
    mongodb.host       = localhost
    mongodb.port       = 27017
    mongodb.database   = conga
